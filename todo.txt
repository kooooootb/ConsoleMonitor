+ craete to-do file
+ monitor do
+ parse command in basecommand
+ parser wrong throws something
+ moditor do exit help
+ parser doenst know its invalid
+ cmake dont include dirs that monitor includes
+ enable input in "\""
+ do help file more pretty
- prompt message choose message what will be shown? default " monitor :"
+ do tests for each commands
+ do all command classes
+ do command factory
+ parser to parse whole line
+ do all values strings
+ do command setting values return const char
+ library of messages
test command functionality
+ do separate vector for bool values
/ bool + pos values == key value
? wrap util in namespace
monitor output to output data monitor input to get data make em mutable and mark function const
